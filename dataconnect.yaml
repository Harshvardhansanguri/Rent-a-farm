specVersion: "v1beta"
serviceId: "rentafarm"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rentafarm-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
